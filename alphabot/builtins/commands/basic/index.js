const command = require('./command');
const vanish = require('./vanish');
const commands = require('./commands');
const banphrase = require('./banphrase');

module.exports = {
  command,
  vanish,
  commands,
  banphrase
}
